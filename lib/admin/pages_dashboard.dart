import 'package:flutter/material.dart';

class PagesDashboard extends StatelessWidget {
  const PagesDashboard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Pages'),);
  }
}
