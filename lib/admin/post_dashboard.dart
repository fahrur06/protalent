import 'package:flutter/material.dart';

class PostDashboard extends StatelessWidget {
  const PostDashboard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Post'),);
  }
}
