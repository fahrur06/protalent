import 'package:flutter/material.dart';
import 'package:pro_talent/card.dart';

class SettingDashboard extends StatelessWidget {
  const SettingDashboard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return CardTalent();
  }
}
